@model List<FMSAPI.Models.GetBookPublishData>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Faculty Name
        </th>
        <th>
            Book Name
        </th>
        <th>
            Publish Date
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Faculty_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Publish_Date)
            </td>
          
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.FacultyID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.FacultyID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.FacultyID })*@
            </td>
        </tr>
    }

</table>
